logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Capacitor build artifacts — generated by `npm run cap:sync` from /dist.
# Tracking them causes merge conflicts on every build and bloats the repo.
ios/App/App/public/
android/app/src/main/assets/public/

# Supabase CLI cache — populated by `supabase link`. Project-ref + version
# pins for the linked project; machine-local, not portable.
supabase/.temp/
